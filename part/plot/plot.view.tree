$bog_vmap_part_plot $mol_view
	values /number
	title \
	color \
	sub /
		<= Title $mol_view
			sub / <= title
		<= Chart $mol_chart
			graphs /$mol_plot_graph
				<= Line $mol_plot_line
					series_x <= series_x /number
					series_y <= values
					color <= color
					title <= title

# $bog_vmap — передача дел

Читать первым. Состояние на 05.09.2026.

## Что прочесть и в каком порядке

**До первой строки кода, целиком:**

1. этот файл;
2. [ARCHITECTURE.md](./ARCHITECTURE.md) — свод решений. Не справочник: каждое утверждение
   там подтверждено замером, и менять их можно только замером. Разделы 1 (документ как один
   класс, провода, пять капканов), 3 (рантайм-компиляция), 4 (граница и два бандла) —
   фундамент, остальное поймёшь по ходу;
3. [PLAN.md](./PLAN.md) — этапы, что закрыто и **чем именно доказано**; в конце визуальный
   долг и состояние по плёнке;
4. [TESTING.md](./TESTING.md) — как гонять тесты, когда в паке несколько исполнителей.

**Память проекта** (`~/.claude/projects/-Users-cmyser-code-mam/memory/`), сгруппировано:

- про проект: `project_bog_vmap`
- **фундамент, без этого нельзя:** `mol_runtime_view_tree_compile` (компиляция в браузере,
  `web.view.tree` пака), `mol_two_bundles_one_page` (два бандла уживаются, ломается только
  `instanceof`), `mam_test_html_masks_missing_deps`
- **`view.tree`:** `mol_view_tree_chain_operator` (провод это `=`, а не `<=`),
  `mol_view_tree_dict_override_replaces` (`attr *` без `^` заменяет словарь базы)
- **ловушки `@$mol_mem`, за день сработали трижды:** `mol_mem_write_freezes_deps` (путь
  чтения не делит ячейку с путём записи), `mol_mem_stamps_owned_class`,
  `mol_mem_created_objects_destroyed`
- **Гипер База:** `giper_baza_mem_accessor_stale`, `giper_baza_save_in_fiber`
  (и почему `@$mol_action` там не годится)
- **сборка MAM:** `mam_same_file_inheritance_loses_body`, `mam_dep_graph_jsdoc_only`
- **браузер и DOM:** `iframe_transparency_color_scheme`, `mol_frame_sandbox_attr_order`,
  `mol_view_transform_transition`, `mol_scroll_single_child`, `mol_string_setter_throw_silent`
- **проверка глазами:** `chrome_automation_frozen_renderer` (вкладка врёт про события),
  `chrome_cdp_probe_without_extension` (запасной стенд)
- **если запускаешь исполнителей:** `mol-subagent.md` — шаблон промпта, правила ведения
  работы и гочи дев-сервера уже внутри

**Донорский код**, читать перед задачами, где он назван: `hyoo-ru/studio.hyoo.ru` (пак
`studio` в `hyoo/hyoo.meta.tree`). Половина фундамента взята оттуда, см. раздел 2
архитектуры. Клонировать заново, каталог сессии временный.

**Исходники `mol`, куда точно придётся заглянуть:**

| файл | зачем |
|---|---|
| `mol/view/tree2/class/props.ts:41` | хак `upper` — почему под-виды плоские свойства корня |
| `mol/view/tree2/to/js/js.ts` | `call_of` (одно звено) и ветка `'='` (цепочка) |
| `mol/view/view/view.tsx:307` | `render()` и `instanceof` — почему документ монтируется узлом |
| `mol/wire/atom/atom.ts:171` | клеймение владеемого значения |
| `mol/build/build.node.ts:1591` | порядок `.view.ts` после `.view.tree` |
| `mol/touch/touch.view.ts` | камера: `pan`, `zoom`, перевод в мировые координаты |

**Стенды спайков:** `spike/s5` (грабля MAM с наследованием внутри файла, стоит трёх файлов),
`spike/s4` (изоляция и два бандла — там измерено то, на чём стоит раздел 4).

---

## 1. Что это

Визуальный конструктор сайтов на бесконечном холсте. Компоненты кладутся мышью, связываются
друг с другом реактивно, документ выгружается настоящим MAM-модулем.

**Одна ставка, из которой следует почти всё остальное: документ vmap — это один класс
`view.tree`.** Не модель, из которой можно сгенерировать код, а сам исходник, который
редактор парсит, правит через AST и сериализует обратно.

Отсюда бесплатно получаются три вещи: экспорт (источник истины и есть результат), обратная
совместимость (чужой `.view.tree` просто открывается), реактивность (связи это штатные
`<=` и `=`, считает их `$mol_wire`).

За день эта ставка окупилась четырежды: провода без машинерии, адресация выделения одним
именем, инспектор без адаптера, палитра без реестра. Если решение кажется требующим
прослойки — сначала проверь, не решается ли оно тем, что узлы уже плоские свойства корня.

---

## 2. Что работает

Открывается на `http://localhost:9080/bog/vmap/app/-/index.html`, в проде на
`https://b-on-g.github.io/vmap/`. Страница в проекте одна, это она.

Слева палитра из 449 классов с поиском и портами, посередине холст с камерой, справа
инспектор свойств. Компонент перетаскивается из палитры на холст и рисуется **настоящим
паковым классом внутри песочницы**; выделяется, двигается, удаляется; свойства правятся, и
правка доезжает до сцены. Документ выгружается паком, который собирается `npx mam` без
единой правки руками.

**Круг замкнут:** выгруженный документ читается как донорский пак для следующего. Проверено
на фикстуре `demo/`, собранной самим экспортом.

### Модули

| | что |
|---|---|
| `lang/` | модель над AST: узел, свойство, документ из нескольких классов |
| `lib/` | библиотека компонентов из донорского пака; `lib/land/` — из ленда Базы |
| `asset/` | файлы отдельными blob-лендами, схема `asset:` |
| `bridge/` | протокол хост↔сцена, направленный, со сверкой личности пира |
| `scene/` | песочница: компиляция, рендер, culling, замер габаритов, сетка |
| `app/` | холст, палитра, инспектор, схема Базы, экспорт |
| `demo/` | приёмочная фикстура экспорта, руками не править |
| `spike/` | стенды спайков; `s5` документирует граблю MAM и стоит того, чтобы остаться |

---

## 3. Как работать

### Сборка

**Собирать своим процессом, общий дев-сервер не дёргать.** Он один на всех, и дважды за
сутки вставал намертво: сперва на потолке кучи после нескольких пересборок подряд, потом
на строке запроса с закодированной папкой `-/` внутри параметра. Сервер оставляем
пользователю — смотреть глазами.

```
( npx mam bog/vmap/<модуль> > /tmp/b.log 2>&1 & p=$!; ( sleep 400; kill -9 $p 2>/dev/null ) & w=$!; wait $p; echo "exit:$?"; kill $w 2>/dev/null )
head -c 200 bog/vmap/<модуль>/-/web.audit.js; head -c 200 bog/vmap/<модуль>/-/node.audit.js
```

**Приёмка сборки — тремя проверками разом:** код выхода, наличие ОБОИХ аудитов, их время.
Почему так — [TESTING.md](./TESTING.md), разделы 3 и 4. В логе штатно мелькает
`git pull --deepen=1 status 1`, это не ошибка сборки.

Правила, каждое стоило кому-то часа:

- **`-/index.html` материализуется только запросом без query.** Пока файла нет, `?что-угодно`
  идёт мимо build-on-demand роута и даёт 404 — легко решить, что сервер не умеет query. Умеет.
- **Смотреть ОБА аудита**, `web.audit.js` и `node.audit.js`. Замерено: web зелёный, node на
  том же модуле падает `TS2322` в `.view.css.ts`. Только web — дыра в тот класс ошибок,
  который поймает CI, а ты нет.
- **Зелёный аудит доказывает тот код, что был на момент сборки.** Сверять mtime `web.js` с
  исходниками.
- **Красный тест в ЧУЖОМ модуле — сперва пересобери его начисто.** Бандл соседа отстаёт от
  его исходника, и падение показывает вчерашний код. Дважды за день поднимали ложную тревогу.

### Проверка глазами

**С 05.09.2026 исполнители в браузер не ходят.** Приёмка задачи — мол-тесты в `-/node.test.js`
затронутых модулей и оба аудита. Смотрит глазами только пользователь, по короткому описанию
«что должно измениться на экране». Ниже — заметки для ведущего, не для исполнителей.

Расширение claude-in-chrome лежит. Запасной путь — свой Chrome с `--remote-debugging-port`
и клиент на голой ноде:

```
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --remote-debugging-port=9333 --user-data-dir=/tmp/cdp-vmap about:blank &
```

Скрипты стенда лежали в каталоге сессии `ca696c81-…/scratchpad/` (`cdp.mjs`, `px.mjs`);
каталог временный, при нужде переписать. Вкладка под автоматизацией бывает с задушенным
рендер-циклом: `rAF` не тикает, события не приходят, а скриншоты рисуются нормально.

### Тон работы

Что за день окупилось и стоит держать:

- **мерить до того, как утверждать.** Правдоподобное объяснение механизма — не проверка.
  Типовая ошибка звучит как «без этой правки было бы X» без единого прогона с X, и всегда
  в свою пользу. За день девять таких утверждений отозвано после замеров, включая мои;
- **отклоняться от задания и называть причину**, а не спрашивать разрешения и не молчать.
  Ведущий ошибается; за день его поправили шесть раз по существу;
- **отчитываться промежуточно**, до кода — план ловит ошибку дешевле, чем готовый код;
- **не строить поверх бага.** Дважды исполнитель отказывался покрывать случай, который
  упирался в чужой дефект, и называл это вслух. Это правильно;
- **ложный отчёт о поломке фундамента дороже любой правки.** Прежде чем писать «сломана
  гарантия», проверь свой стенд: заглушка вместо бандла, протухший бандл, артефакт
  автоматизации. Все три случая за день были.

---

## 4. Что не закрыто

### Визуальный долг

Расширение браузера легло на середине дня, и приёмка глазами накопилась. Полный список —
в конце [PLAN.md](./PLAN.md). Снимается стендом CDP.

Из него **проверено**: плашка «сцена не отвечает» работает (`stalled: true`, статус на
экране, холст возвращается после перезагрузки кадра).

### Плёнка на холсте — главное незакрытое

После пана фон холста однородно светлеет: поле `rgb(11,23,33)` → `rgb(40,49,57)`, линии
сетки `rgb(26,36,45)` → `rgb(51,59,66)`.

Замерено в живом окне, `rAF` тикает, пан настоящим жестом. **Исключено:** подъём слоя сам по
себе, `color-scheme`, CSS пака, что-либо красящее внутри сцены. **Необъяснено:** один и тот
же объявленный `oklch(0.2 0.03 240)` красится `rgb(11,23,33)` в хосте и на контрольной
странице, но `rgb(38,48,56)` в документе сцены.

Правка «объявить `color-scheme: dark` и перенести сетку в сцену» плёнку **не лечит** и
подлежит откату, если не найдётся другой причины её оставить. Сейчас она в рабочем дереве
незакоммиченной.

### Известные щели

- **Переименование класса из инспектора** осиротит выделение и координату на холсте: узел
  переименуется, а `selected()` и ключ в расстановке останутся старыми. Сегодня недостижимо.
- **Две копии топологической сортировки** — канонический экземпляр в `lang`, своя у сцены.
  Свести, пока не разъехались: расхождение даст `Class extends value undefined` не сразу.
- **Версионирование ленд-библиотек** острее, чем у паков: земля мутабельна, правка автора
  приезжает всем сославшимся без их ведома. Решать до того, как ими начнут пользоваться вдвоём.

---

## 5. Что дальше по плану

Этап 2 закрыт целиком. Вне очереди сделаны экспорт (7.1, 7.3) и модели файлов и
ленд-библиотеки (5.1, 5.2, 5.4).

Осталось, и всё это интерфейс: этап 3 — провода мышью на холсте; этап 4 — редактор кода с
горячей правкой; этап 6 — артборды. Плюс UI загрузки файла и публикации компонента.

**Артборды меняют модель размещения**, и до них расстановка на холсте — строительные леса:
координаты едут отдельным каналом `spots_set` и в экспорт не попадают намеренно.

---

## 6. Git

Пак — отдельный репозиторий, как и все приложения в `bog/`; сам `bog/` только реестр паков и
приложений не содержит. `pack vmap` в `bog.meta.tree` уже прописан.

**Remote есть с 08.09.2026:** `origin` → `https://github.com/b-on-g/vmap`, публичный,
ветка **`main`** (именно main, не master). Пушить свои коммиты самому.

Коммитить и пушить — самому, не спрашивая, сообщение по существу правок. Пометок про Claude
не ставить нигде.

## 7. Деплой

Пуш в `main` запускает `.github/workflows/deploy.yml`: `mam_build@master2` собирает три
модуля (`app scene part`), затем `gh-deploy@v4.4.1` кладёт **редактор в корень сайта**, а
`scene/-` и `part/-` подпапками. Витрины больше нет: вести с неё некуда, страниц у соседей
не осталось. Адреса:

| | |
|---|---|
| `https://b-on-g.github.io/vmap/` | редактор, единственная страница проекта |
| `/vmap/scene/web.js` | бандл песочницы, его тянет разметка кадра |
| `/vmap/part/web.js` | бандл пака деталей, его тянет сцена |
| `/vmap/part/web.view.tree` | дерево классов пака, его читает палитра |

Три вещи, которые в экшене неочевидны и каждая стоила кому-то деплоя:

- **`clean-exclude` в корневом шаге обязателен.** `rsync --delete` в корне снесёт папки
  модулей, которые кладутся следующими шагами;
- **cache-bust бандла стемпом коммита.** Pages отдаёт `web.js` с `max-age=600`, и первые
  десять минут вернувшийся читатель получает старый бандл. Штампуется одна страница,
  потому что она одна;
- **`.gitattributes` с `* -text`** — без него сборка в экшене ломается на переводах строк.

**Адреса сцены и своего пака никуда не зашиты.** Раскладка читается по адресу самой
страницы одной чистой функцией `$bog_vmap_lib_sibling` в `lib/lib.ts`: хвостовой сегмент
`-` значит дев-сервер, там модули лежат рядом, каждый в своём `-/`; без него это деплой, и
модуль это папка внутри той, из которой отдан редактор. Из неё выводятся и `scene_bundle`,
и адрес пака при пустом поле палитры. Пустое поле палитры значит «свой пак `part`».

**Страница в проекте одна — `app/index.html`.** У сцены и у пака деталей своей нет и быть
не должно; кадр сцены поднимается разметкой, см. раздел 4 архитектуры. Единственное
исключение на диске — `demo/index.html`, но это не страница проекта, а **выход** экспорта:
её пишет сам экспорт, и без неё выгруженный модуль остался бы без точки входа.
